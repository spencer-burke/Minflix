services:
  # Production API service (serves both API and frontend)
  api:
    build:
      context: .
      dockerfile: dockerfile.prod
    ports:
      - "8000:8000"
    environment:
      - PYTHONPATH=/app
    restart: unless-stopped 